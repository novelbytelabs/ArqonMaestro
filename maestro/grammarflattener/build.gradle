apply plugin: "application"

application {
    mainClass = "grammarflattener.GrammarFlattener"
}

distZip.enabled = false
distTar.enabled = false

dependencies {
    implementation project(":toolbelt")
}

Wave A Patch 1 + 2 - No Bypass Proof
=====================================

=== Exact Grep/Diff Commands Run ===

1. Check if old inline VAD logic exists in current code:
   $ git diff 2c2a7b7 47c291c -- maestro/client/src/main/audio/index.ts | grep -c "volume >= speechThreshold"
   Result: 0 (the inline comparison is GONE)

2. Check if new provider call exists:
   $ grep -n "vadProvider.process" maestro/client/src/main/audio/index.ts
   Result: 286:      const vadDecision = this.vadProvider.process({

3. Check that speech state is set from provider result:
   $ grep -n "this.speaking = vadDecision" maestro/client/src/main/audio/index.ts
   Result: 296:      this.speaking = vadDecision.isSpeech;

=== Exact Outputs ===

--- Baseline (2c2a7b7) - Inline VAD Logic ---
Lines 202-225 of audio/index.ts:
```
const volume = this.rms(audio);
const silenceThreshold = this.effectiveSilenceThreshold();
const speechThreshold = this.effectiveSpeechThreshold();

if (volume >= speechThreshold) {
  this.consecutiveSpeech += 1;
  if (this.consecutiveSpeech >= this.consecutiveFramesForSpeaking) {
    this.speaking = true;
  }
} else if (volume <= silenceThreshold) {
  this.consecutiveSpeech = 0;
  this.consecutiveSilence += 1;
  if (this.consecutiveSilence >= this.silenceFramesToEnd) {
    this.speaking = false;
  }
} else {
  this.consecutiveSpeech = 0;
  this.consecutiveSilence = 0;
}
```

--- Current (47c291c) - Provider-Based VAD ---
Lines 286-297 of audio/index.ts:
```
const vadDecision = this.vadProvider.process({
  pcm16: denoisedAudio,
  sampleRate: audioFrame.sampleRate,
  channels: audioFrame.channels,
  frameIndex: audioFrame.frameIndex,
  timestampMs: audioFrame.timestampMs,
  streamTimeMs: audioFrame.streamTimeMs,
});

// Use VAD provider result for speech state
this.speaking = vadDecision.isSpeech;
this.noiseFloor = vadDecision.noiseFloor;
```

=== Explanation ===

The recorder path CANNOT bypass vadProvider.process() because:

1. The OLD inline VAD logic (volume comparisons against thresholds) has been REMOVED
   from audio/index.ts. It only exists now inside DefaultVadProvider.process().

2. The ONLY path to set this.speaking is through:
   - Line 286: Call this.vadProvider.process(...)
   - Line 296: Set this.speaking = vadDecision.isSpeech

3. There is no fallback path. If you remove the provider call,
   this.speaking will never be updated and all audio will be treated as silence.

4. The diff confirms the inline comparisons are gone:
   - "volume >= speechThreshold" count: 0 in current code
   - "volume <= silenceThreshold" count: 0 in current code

=== Verification Steps for Auditor ===

To verify this independently:

1. Look at audio/index.ts lines 286-297
2. Confirm that this.speaking is ONLY set from vadDecision.isSpeech
3. Confirm no other code path can set this.speaking
4. Look at DefaultVadProvider.process() to see where the logic moved
5. Compare threshold values between baseline and provider (both use 0.008/0.015)

# Prompt for the New Assistant Instance

You are resuming a long-running H3 / Arqon Maestro / Arqon Manifold project.
Read the transition context pack first, then use the real repo and `new_instance.zip` as source of truth.

Your objectives:
1. recover exact project state without losing doctrine
2. continue the implementation mainline and documentation recovery in parallel
3. preserve branch/baseline correctness
4. avoid reintroducing old bundle/integration mistakes

Critical operating assumptions:
- main H3 repo work is in ArqonMaestro
- working branch has been `feature/h3`
- 3D3 closed
- 3E1 closed
- 3E2 closed
- 3F closed
- 3G active and deep into implementation, with S1-S5 reported green in real repo
- 3H-S1 user-confirmed done, but fresh instance must verify exact repo state before further 3H work

Doctrine:
- live voice geometry proposes
- focus/task reshapes ranking and legality
- memory supplies priors
- governance decides execution
- no H23/H24 bypass
- no Stage 3A activation drift
- warm/focus/shard/task-history/counterfactual signals may shape or accelerate but may not authorize
- protobuf for internal communication
- JSON only for human-facing surfaces
- no silent mocks/stubs/shims/placeholders

Process discipline:
- stop on first failure during real local gates
- local gates are the proof
- bundle-local verification is advisory only
- when repo and docs disagree, repo wins
- when earlier bundle state and repaired real-repo state disagree, repaired real-repo state wins

First actions for the new instance:
1. inspect `transition_pack_context` docs
2. inspect `new_instance.zip`
3. inspect actual repo branch / HEAD / status
4. verify exact status of:
   - 3G closure need
   - 3H-S1 real commit/gates
5. continue from the actual repo baseline, not from stale bundle assumptions

Expected messaging style to user:
- direct
- operational
- truthful
- explicit about what is verified versus assumed
- enthusiastic only when evidence is real

Expected messaging style to other AI:
- authoritative
- exact
- no ambiguity
- exact files, exact gates, stop-on-first-failure

Current likely agenda:
- verify 3H-S1 repo truth
- continue 3H mainline
- keep docs recovery active
- maintain artificial surfaces control register
- maintain validation gates discipline
